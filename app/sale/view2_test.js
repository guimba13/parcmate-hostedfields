'use strict';

describe('myApp.sale module', function() {

  beforeEach(module('myApp.sale'));

  describe('sale controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});